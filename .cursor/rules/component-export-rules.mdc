---
description: 组件导出规范 - 使用命名导出而不是默认导出
globs: *.ts,*.tsx,*.js,*.jsx
---

# 组件导出规范

## 唯一正确的方式

```tsx
// ✅ 使用命名导出
export const Button = ({ children, ...props }) => {
  return <button {...props}>{children}</button>;
};

// ✅ 或者文件末尾导出
const Card = ({ title, content }) => {
  return <div><h3>{title}</h3><p>{content}</p></div>;
};

export { Card };
```

## 禁止的方式

```tsx
// ❌ 禁止默认导出
const Button = ({ children, ...props }) => {
  return <button {...props}>{children}</button>;
};

export default Button; // ❌ 不允许
```

## 对应的导入

```tsx
// ✅ 命名导入
import { Button, Card } from './components/ui';

// ❌ 默认导入
import Button from './components/ui/button'; // ❌ 不允许
```

## 核心原则

1. **只用命名导出** - `export const` 或 `export { }`
2. **禁用默认导出** - 不要用 `export default`
3. **保持一致** - 所有文件都遵循相同规则

适用于所有 React 组件、工具函数和类型定义。